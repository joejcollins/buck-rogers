# ~/~ begin <<docs/max_by_math.md#src/buck_rogers/max_math.py>>[init]
"""Max value of two numbers."""

def find_max_math(a, b):
    """Compute the maximum of two integers using arithmetic operations only."""
    return (a + b + abs(a - b)) // 2
# ~/~ end
